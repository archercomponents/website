<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Archer Components" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Archer Components builds high-quality low-cost mountain bike components" %>">
    <meta name="keywords" content="mountain bike bicycle downhill mtb electronic shifting shifter digital wireless components">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <% if false %><%= render 'layouts/navigation' unless current_page?(root_path) %><% end %>
    </header>
      <%= render 'layouts/messages' %>
      <%= yield %>
  </body>
</html>
